# 贡献说明

本仓库面向后续同学继续开发无人机自主飞行功能。贡献代码前应先完成无桨测试，涉及上桨飞行的改动必须在空旷、安全、可人工接管的环境中验证。

## 代码原则

- 保持飞行控制脚本的小步迭代，不把未验证的大规模重构直接合入主线。
- 新增飞行控制逻辑时保留遥控器接管入口，至少保持 `GUIDED + CH6 high + CH7 high + armed` 这类门控思想。
- 新增视觉配置时写清楚相机、分辨率、ArUco 尺寸、布局几何和安装外参。
- 新增依赖时同步更新根目录依赖文件，并确认核心脚本 `--help` 可以运行。
- 不提交 `.pyc`、`__pycache__`、日志、视频、完整标定图片集或临时备份文件。

## 提交流程

1. 在本地完成静态检查：确认仓库中没有缓存、备份和大文件。
2. 运行飞控或视觉相关脚本的 `--help`，确认依赖和入口没有破坏。
3. 对硬件相关改动完成无桨验证，记录关键日志。
4. 对视觉相关改动完成相机识别和位姿输出验证，记录使用的配置文件。

